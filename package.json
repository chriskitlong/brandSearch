{
  "name": "pern",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd api && npm install && node server.js",
    "build": "cd ui && yarn && yarn run build",
    "heroku-postbuild": "cd ui && yarn && yarn run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
